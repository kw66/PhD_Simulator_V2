/**
 * Temporary development switch for inspecting unfinished panels before
 * enrollment. Set to false before release to restore the enrollment gate.
 */
export const SHOW_ALL_MODULES_DURING_DEVELOPMENT = true;
